import { StaticImageData } from 'next/image'
import { fbIcon, instaIcon, kIcon, xIcon, youtubeIcon } from './assets'

export const icons: StaticImageData[] = [
  kIcon,
  instaIcon,
  youtubeIcon,
  fbIcon,
  xIcon,
]
